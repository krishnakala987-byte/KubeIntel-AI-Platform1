apiVersion: v1
kind: Service
metadata:
  name: kubeops-ui-service
  namespace: kubeintel
spec:
  selector:
    app: kubeops-ui
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30082
  type: NodePort